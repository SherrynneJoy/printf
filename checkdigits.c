#include "main.h"

/**
 * is_digit - checks for a digit
 * @c: character to be evaluated
 * Return: 0 when successful
 */

int is_digit(char c)
{
	if ('c' >= 0 && 'c' < 10)
		return (1);

	return (0);
}
